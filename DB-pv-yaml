apiVersion: v1
kind: PersistentVolume
metadata:
  name: masktv-db-pv
  labels:
    app: masktv-db
spec:
  capacity:
    storage: 8Gi
  accessModes:
    - ReadWriteMany
  storageClassName: aws-ebs
  awsElasticBlockStore:
    volumeID: <db-volume-id>
    fsType: ext4
  persistentVolumeReclaimPolicy: Recycle
