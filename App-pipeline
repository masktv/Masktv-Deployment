pipeline {
  agent any
  stages {
    stage('Checkout') {
      steps {
        git 'https://github.com/your-repo.git'
      }
    }
    stage('Build Docker Image') {
      steps {
        script {
          docker.build('app-image', '-f app.dockerfile .')
        }
      }
    }
    stage('Push Docker Image') {
      steps {
        script {
          docker.withRegistry('https://index.docker.io/v1/', 'dockerhub-credentials-id') {
            docker.image('app-image').push('latest')
          }
        }
      }
    }
    stage('Deploy to Kubernetes') {
      steps {
        script {
          sh 'kubectl apply -f app.pv'
          sh 'kubectl apply -f app.pvc'
          sh 'kubectl apply -f app.deployment'
          sh 'kubectl apply -f app.service'
          sh 'kubectl apply -f ingress.yaml'
          sh 'kubectl apply -f app.hpa'
        }
      }
    }
  }
}
